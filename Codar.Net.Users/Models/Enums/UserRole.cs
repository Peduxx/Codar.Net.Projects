using System.Collections.Generic;

namespace Codar.Net.Users.Models.Enums
{
    public enum UserRole
        {
            FreeUser = 0,
            PremiumUser = 1,
            AdminUser = 2
        }
}